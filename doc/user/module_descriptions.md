# Evaluation framework modules
The evaluation framework has a modular architecture where the different steps are derived from base classes.

Each section of the evaluation pipeline has a base class to provide the interface for that section. 

There are already some modules provide with the framework distribution but it is left to you you to decide whether a new module should be implemented

The sections are 
 
- [Data generators](data_generators.md)
